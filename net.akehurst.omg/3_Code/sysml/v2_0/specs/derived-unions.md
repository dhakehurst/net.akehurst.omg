# Derived-union report — SysML (v2.0 specs)

Source XMI files: `SysML_2-0_ptc-25-02-15.xmi`, `sysml_patched.xmi`

Summary
- No occurrences of `isDerivedUnion="true"` were found under the `sysml` specs folder in this workspace view.

Details
- A search for `isDerivedUnion` under `/3_Code/sysml/v2_0/specs` returned no matches.

Recommendation
- No derived-union implementations are required for the SysML specs present in this folder.

