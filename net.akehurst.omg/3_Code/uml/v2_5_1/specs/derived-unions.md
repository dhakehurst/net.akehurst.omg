# Derived-union report — UML (2.5.1 assembly)

Source XMI file: `UML_2-5-1_ASM_ptc-18-01-01.xmi` (assembly)

Summary
- The UML assembly XMI contains many `isDerivedUnion="true"` occurrences (elements such as `Element`, `Namespace`, `ActivityGroup`, `ActivityEdge`, `ActivityNode`, `NamedElement`, `Relationship`, etc.).
- The XMI also contains numerous explicit `<subsettedProperty xmi:idref="..."/>` entries that reference these derived-union attributes. This makes many UML derived-unions amenable to automated generation, but caution is required because of OCL constraints, ordering/uniqueness differences, and redefinition rules.

Examples (not exhaustive)
1) `Element::ownedElement` (xmi:id="Element-ownedElement")
- Defined as: `<ownedAttribute ... xmi:id="Element-ownedElement" name="ownedElement" isReadOnly="true" ... />`
- Subset references found (examples in the assembly XMI include):
  - multiple `<subsettedProperty xmi:idref="Element-ownedElement"/>` occurrences (these appear across the assembly file at different positions)
- Complexity: MEDIUM. Because the file explicitly lists subsettedProperty references, auto-generation is possible if the generator verifies compatibility and absence of additional OCL filtering.

2) `Element::owner` (xmi:id="Element-owner")
- Subset references found in the assembly (e.g., many `<subsettedProperty xmi:idref="Element-owner"/>` lines).
- Complexity: MEDIUM.

3) Activity-related derived unions
- Examples: `Activity-group` (Activity.group), `ActivityNode-inGroup`, `ActivityEdge-inGroup` — the assembly uses `isDerivedUnion="true"` for these and contains `subsettedProperty` entries like `Activity-group`, `Activity-node`, `ActivityEdge-inGroup`.
- Complexity: MEDIUM to HIGH depending on whether ordering/aggregation semantics or OCL filters apply.

Notes on analysis
- The UML assembly file is large; grep shows many `isDerivedUnion` occurrences and corresponding `subsettedProperty` idrefs such as:
  - `Element-ownedElement`, `Element-owner`
  - `Activity-group`, `Activity-node`, `ActivityEdge-inGroup`
  - `Namespace-ownedMember`, `Namespace-ownedMember` references
  - `NamedElement` / `Element` / `Relationship` / `DirectedRelationship` derived-unions
- Because subset sources are enumerated explicitly in the XMI, a generator can collate the source properties and produce an autogenerated getter that unions them. However, generators must handle:
  - collection-kind reconciliation (List / Set / OrderedSet / Collection)
  - preserving ordering when `isOrdered` is true
  - preserving uniqueness when `isUnique` is true/false
  - unresolved-reference semantics for collection-of-references (REQ-2.7.8)
  - potential OCL `specification` filters and redefinition chains

Recommendation
- Emit a per-model derived-union report (this file) and for each derived-union attribute either:
  - Generate an auto-derived getter when safety checks pass (explicit subset list, compatible types and collection semantics, no OCL filters), or
  - Otherwise generate a TODO stub and include the explicit subset source list to help implementers write correct logic by hand.

If you want, I can produce a more detailed, per-attribute mapping (JSON or CSV) enumerating every derived-union attribute id in this assembly and the full set of `subsettedProperty` idrefs that target it. That mapping enables fully-automated generation of suggested getters (optionally gated behind a generator flag).

